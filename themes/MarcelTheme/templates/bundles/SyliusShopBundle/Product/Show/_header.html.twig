<h1 id="sylius-product-name" class="ui monster dividing header" {{ sylius_test_html_attribute('product-name', product.name) }}>
    {{ product.name }} <br>
</h1>
{% if currentTableauCode is defined %}
    Tableau {{currentTableauCode}}
    {% endif %}

{% if productAssocie is defined %}
    <div class="ui centered sub header" style="font-size: 2em; margin-bottom: 1em;">
        🖼️ Imprimé avec le tableau: </br><strong>"{{ productAssocie.name }}"</strong>
    </div>
{% endif %}

